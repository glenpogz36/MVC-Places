@using PlaceList.Models;

<h1>List of Places</h1>
<ul>

@if (Model.Count == 0)
{
  <p>You have no locations in your travel journal!</p>
}
@if(Model.Count != 0)
{
  @foreach (Place place in Model)
  {
     <li><a href='/places/@place.GetId()'>@place.GetCityName()</a></li>
  }
}

</ul>
<a href="/places/new">Add a new place.</a>

<form action="/places/delete" method="post">
  <button type="submit" name="button">Clear All Places</button>
</form>


